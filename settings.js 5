// settings.js

module.exports = {
  // Aktifkan atau nonaktifkan fitur Auto Read Status
  autoReadStory: true, 

  // Aktifkan atau nonaktifkan fitur Auto Typing
  autoTyping: false,

  // Aktifkan atau nonaktifkan fitur Update Bio (Aktif Selama Uptime)
  bioActive: false,

  // Aktifkan atau nonaktifkan fitur Delete Status
  deleteStatus: false, 

  // Kecepatan melihat status (dalam milidetik, minimal 1 detik)
  readStatusSpeed: 500, // 2 detik

  // Aktifkan atau nonaktifkan tampilan statistik status saat bot dimulai
  showStatistics: true,

  // Aktifkan atau nonaktifkan tampilan total view status 
  showViewStatus: true,

  // Aktifkan atau nonaktifkan tampilan total delete status
  showDeleteStatus: false,

  // Aktifkan atau nonaktifkan reset Total View Status setiap hari
  resetViewStatusDaily: true
};
