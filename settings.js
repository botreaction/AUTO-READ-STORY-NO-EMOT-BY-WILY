const readStatusSpeed = 1000; // 1 detik (1000 milidetik)
const autoReadStory = true; // Aktifkan auto read story secara default
const bioActive = false; // Aktifkan auto bio secara default
const autoTyping = false; // Nonaktifkan auto typing secara default

module.exports = { readStatusSpeed, bioActive, autoReadStory, autoTyping };
